Do we have permissions to read:
- /etc/shadow

Program to merge /etc/passwd and /etc/shadow:
```bash
unshadow <PASSWORD-FILE> <SHADOW-FILE>
```

Put the username:password-hash lines in a file and use hashcat to crack them.